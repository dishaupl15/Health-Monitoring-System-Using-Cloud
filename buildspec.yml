version: 0.2

phases:

  install:
    runtime-versions:
      nodejs: 20

    commands:
      - cd agentic-health-monitor/frontend
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'

  base-directory: agentic-health-monitor/frontend/dist